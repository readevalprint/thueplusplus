name = "kv rejects malformed command arguments"
program = "../kv.tpp"
input = "set onlykey"

[bindings.files]
db = { fixture = "fixtures/kv-db.state" }

[expect]
stdout = "ERR:invalid_input\n"
stderr = ""
exit_code = 1
