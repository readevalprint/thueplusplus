name = "transactional delete tombstone hides parent value"
program = "../cow-kv.tpp"
input = "set a base; begin; del a; get a; discard; get a; begin; del a; commit; get a"

[expect]
stdout = "nil\nbase\nnil\n"
stderr = ""
exit_code = 0
