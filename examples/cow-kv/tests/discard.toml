name = "copy-on-write discard restores parent value"
program = "../cow-kv.tpp"
input = "set a base; begin; set a child; get a; discard; get a"

[expect]
stdout = "child\nbase\n"
stderr = ""
exit_code = 0
