# SPDX-License-Identifier: AGPL-3.0-or-later
program = "fixtures/bad-template-arg.tpp"
timeout = 5

[[case]]
name = "malformed template is rejected as a builtin argument"

[case.expect]
exit_code = 1
stderr_contains = ["Builtin arg '{{b|}}' is not a capture name"]
