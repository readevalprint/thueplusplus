name = "line read rejects non-numeric timeout token"
program = "../invalid-line-timeout.tpp"
timeout = 10

[bindings.procs]
worker = "printf 'alpha\n'"

[expect]
stdout = ""
stderr = "Error: Line 2: invalid read timeout 'n'\n"
exit_code = 1
