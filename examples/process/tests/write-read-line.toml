name = "write then newline read through process"
program = "../write-read-line.tpp"
timeout = 10

[bindings.procs]
worker = "python3 -u examples/process/tests/fixtures/echo_worker.py"

[expect]
stdout = "reply:ping"
stderr = ""
exit_code = 0
