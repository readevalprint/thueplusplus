test:
  image: python:3.11-slim
  script:
    - python3 -m pip install --disable-pip-version-check .
    - python3 -m py_compile python/thuepp.py python/tests/*.py
    - python3 -m unittest discover -s python/tests -v
